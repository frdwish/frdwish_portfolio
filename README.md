
  # Personal Portfolio Website

  Building this will be pretty much hard but i will have fun.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
